# Image Grammar — Parametric Color Encoding Structure
#
# Structural grammar for encoding binary payloads as color sequences
# rendered into an image. Follows the same architecture as music (MIDI
# note sequences): the grammar generates a structural frame, payload
# colors fill the N slots, cover tokens provide visual context (shapes,
# styles, row markers, headers).
#
# Output structure (raw dialect):
#   <payload colors>
#
# Output structure (grid dialect):
#   size=256 cols=4 palette=viridis\n
#   <rows of color cells separated by row markers>
#   ||
#
# Output structure (voronoi dialect):
#   voronoi: size=400 palette=viridis\n
#   <color cells>
#   ||
#
# Type inventory (reinterpreted for image):
#   e = color (a palette color sample)
#   t = complete visual unit (row, frame, image)
#
# POS reinterpretation for image:
#   N     = palette color (carries data)
#   Adj   = cell shape (cell, patch, dot, tile, triangle, hexagon, pixel)
#   Adv   = style modifier (filled, outlined, stroked, gradient)
#   Dot   = row separator (|) or frame end (||)
#   Cop   = background fill (occupies cell without payload)
#   Aux   = header tokens (size, cols, palette)
#   Modal = rendering hint (smooth, crisp, rounded)
#   Conj  = section separator (\n)
#   Prefix = layout dialect marker (voronoi:, grid:, patches:, mosaic:)
#   To    = border/connection marker

grammar:
  name: "Image Grammar"

  # Image language supports multiple palette sizes (16, 32, 64, 96 colors)
  # with varying capacities. Disable bitpacking to allow non-power-of-two
  # palette sizes for custom palettes.
  bitpacking: false

  # Colors are space-separated tokens (not concatenated like CS base58).
  payload_separator: " "

  # Dot produces row markers, not sentence-ending periods.
  dot_is_punctuation: false

  types:
    - name: "N"
      lambda_type: "e -> t"

    - name: "Adj"
      lambda_type: "e -> e"

    - name: "Adv"
      lambda_type: "(e -> t) -> (e -> t)"

    - name: "Dot"
      lambda_type: "t"

    - name: "Cop"
      lambda_type: "(e -> t) -> (e -> t)"

    - name: "Aux"
      lambda_type: "(e -> t) -> (e -> t)"

    - name: "Modal"
      lambda_type: "(e -> t) -> (e -> t)"

    - name: "Conj"
      lambda_type: "t -> (t -> t)"

    - name: "Prefix"
      lambda_type: "t -> t"

    - name: "To"
      lambda_type: "(e -> t) -> (e -> t)"

  # Base rules — grid layout (analogous to music "scored" with bars)
  #
  # sentence → ROWS Dot[end]
  # ROWS → ROW | ROW ROWS
  # ROW → CELL CELL CELL CELL Dot  (4 cells per row, row marker)
  # CELL → N Adj | Cop Adj          (color+shape or background+shape)
  rules:
    sentence:
      lambda: "λn:(e->t). n"
      cfg_productions:
        - production: "ROWS Dot[end]"
          weight: 1.0
          lambda: "λn:(e->t). n"

    ROWS:
      lambda: "λn:(e->t). n"
      cfg_productions:
        - production: "ROW"
          weight: 0.15
          lambda: "λn:(e->t). n"
        - production: "ROW ROWS"
          weight: 0.85
          lambda: "λn:(e->t). n"

    ROW:
      # 4 cells per row (like 4 beats per bar in music's 4/4 time).
      # This is the default grid width; dialects can override.
      lambda: "λn:(e->t). n"
      cfg_productions:
        - production: "CELL CELL CELL CELL Dot"
          weight: 1.0
          lambda: "λn:(e->t). n"

    CELL:
      # A cell is either a payload color or background, each with a shape.
      # Style modifiers and rendering hints optionally modify colors.
      #
      # One palette color per CELL slot (6 bits/color with N=64).
      # Shapes (cell, patch, dot, etc.) are interpretive — they specify
      # visual rendering but don't change payload capacity.
      lambda: "λn:(e->t). n"
      cfg_productions:
        - production: "N Adj"
          weight: 0.70
          # Payload color + shape: "c23 cell"
          lambda: "λn:(e->t). λadj:(e->e). adj(n)"
        - production: "Cop Adj"
          weight: 0.15
          # Background + shape: "bg cell"
          lambda: "λadj:(e->e). λcop:((e->t)->(e->t)). cop(adj)"
        - production: "Adv N Adj"
          weight: 0.10
          # Style + color + shape: "filled c23 cell"
          lambda: "λn:(e->t). λadj:(e->e). λadv:((e->t)->(e->t)). adv(adj(n))"
        - production: "Modal N Adj"
          weight: 0.05
          # Rendering hint + color + shape: "smooth c23 cell"
          lambda: "λn:(e->t). λadj:(e->e). λmod:((e->t)->(e->t)). mod(adj(n))"

  # Dialect overrides
  dialects:
    raw:
      # Raw color sequence — no row markers, no headers. Just payload colors.
      # Analogous to music "raw" dialect and CS "raw" dialect.
      payload_wordlist: viridis
      rules:
        sentence:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "BODY"
              weight: 1.0
              lambda: "λn:(e->t). n"

        BODY:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "N"
              weight: 0.1
              lambda: "λn:(e->t). n"
            - production: "N BODY"
              weight: 0.9
              lambda: "λn:(e->t). n"

    grid:
      # Grid layout — header with size/cols/palette, structured rows, end marker.
      # Analogous to music "scored" dialect.
      #
      # Output:
      #   size=256 cols=4 palette=viridis
      #   #2a788e cell #22a884 cell #7ad151 cell #440255 cell |
      #   #414487 cell #355f8d cell #fde725 cell #46327e cell |
      #   ||
      payload_wordlist: viridis
      rules:
        sentence:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "HEADER ROWS Dot[end]"
              weight: 1.0
              lambda: "λn:(e->t). n"

        HEADER:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "Aux[size] Aux[cols] Aux[palette] Conj"
              weight: 1.0
              # Header tokens are templates. Renderer interprets:
              #   Aux[size]    → "size=256" (default)
              #   Aux[cols]    → "cols=4"   (default)
              #   Aux[palette] → "palette=viridis"
              #   Conj         → "\n"
              # Output: "size=256 cols=4 palette=viridis\n"
              lambda: "λn:(e->t). n"

        ROWS:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "ROW"
              weight: 0.15
              lambda: "λn:(e->t). n"
            - production: "ROW ROWS"
              weight: 0.85
              lambda: "λn:(e->t). n"

        ROW:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "CELL CELL CELL CELL Dot"
              weight: 0.70
              # Standard 4-cell row
              lambda: "λn:(e->t). n"
            - production: "CELL CELL CELL CELL CELL CELL CELL CELL Dot"
              weight: 0.15
              # Wide 8-cell row
              lambda: "λn:(e->t). n"
            - production: "CELL CELL Dot"
              weight: 0.10
              # Narrow 2-cell row
              lambda: "λn:(e->t). n"
            - production: "CELL Dot"
              weight: 0.05
              # Single-cell row
              lambda: "λn:(e->t). n"

        CELL:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "N Adj"
              weight: 0.60
              lambda: "λn:(e->t). λadj:(e->e). adj(n)"
            - production: "Cop Adj"
              weight: 0.10
              lambda: "λadj:(e->e). λcop:((e->t)->(e->t)). cop(adj)"
            - production: "Adv N Adj"
              weight: 0.15
              lambda: "λn:(e->t). λadj:(e->e). λadv:((e->t)->(e->t)). adv(adj(n))"
            - production: "Modal N Adj"
              weight: 0.10
              lambda: "λn:(e->t). λadj:(e->e). λmod:((e->t)->(e->t)). mod(adj(n))"
            - production: "N Adj To N Adj"
              weight: 0.05
              # Bordered pair: "c03 cell border c15 cell"
              lambda: "λn:(e->t). λadj:(e->e). λto:((e->t)->(e->t)). to(adj(n))"

    voronoi:
      # Voronoi cell layout — the signature visual dialect.
      # Lloyd-relaxed Voronoi cells, each filled with a payload color.
      # Cell borders and seed positions are cover (not payload).
      #
      # The s-value metadata on each token enables similarity-aware
      # relaxation: cells with close s-values get an extra repulsive
      # force, pushing perceptually similar colors apart for visual
      # distinctness and easier decoding.
      #
      # No row markers — the renderer arranges cells as a Voronoi diagram.
      # The CELLS rule is flat (not grid-structured) because Voronoi layout
      # is inherently spatial, not row-based.
      payload_wordlist: viridis
      rules:
        sentence:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "HEADER CELLS Dot[end]"
              weight: 1.0
              lambda: "λn:(e->t). n"

        HEADER:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "Prefix[voronoi] Aux[size] Aux[palette] Conj"
              weight: 1.0
              lambda: "λn:(e->t). n"

        CELLS:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "CELL"
              weight: 0.1
              lambda: "λn:(e->t). n"
            - production: "CELL CELLS"
              weight: 0.9
              lambda: "λn:(e->t). n"

        CELL:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "N Adj"
              weight: 0.75
              lambda: "λn:(e->t). λadj:(e->e). adj(n)"
            - production: "Cop Adj"
              weight: 0.10
              lambda: "λadj:(e->e). λcop:((e->t)->(e->t)). cop(adj)"
            - production: "Adv N Adj"
              weight: 0.10
              lambda: "λn:(e->t). λadj:(e->e). λadv:((e->t)->(e->t)). adv(adj(n))"
            - production: "Modal N Adj"
              weight: 0.05
              lambda: "λn:(e->t). λadj:(e->e). λmod:((e->t)->(e->t)). mod(adj(n))"

    patches:
      # Rectangular patches — quilt-like grid of colored rectangles.
      # Structured with rows, like the base grid, but uses "patch" shape.
      payload_wordlist: viridis
      rules:
        sentence:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "HEADER ROWS Dot[end]"
              weight: 1.0
              lambda: "λn:(e->t). n"

        HEADER:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "Prefix[patches] Aux[size] Aux[cols] Aux[palette] Conj"
              weight: 1.0
              lambda: "λn:(e->t). n"

        ROWS:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "ROW"
              weight: 0.15
              lambda: "λn:(e->t). n"
            - production: "ROW ROWS"
              weight: 0.85
              lambda: "λn:(e->t). n"

        ROW:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "CELL CELL CELL CELL Dot"
              weight: 0.80
              # Standard 4-patch row
              lambda: "λn:(e->t). n"
            - production: "CELL CELL Dot"
              weight: 0.15
              # 2-patch row (large patches)
              lambda: "λn:(e->t). n"
            - production: "CELL Dot"
              weight: 0.05
              # Single large patch
              lambda: "λn:(e->t). n"

        CELL:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "N Adj"
              weight: 0.80
              lambda: "λn:(e->t). λadj:(e->e). adj(n)"
            - production: "Cop Adj"
              weight: 0.10
              lambda: "λadj:(e->e). λcop:((e->t)->(e->t)). cop(adj)"
            - production: "Adv N Adj"
              weight: 0.10
              lambda: "λn:(e->t). λadj:(e->e). λadv:((e->t)->(e->t)). adv(adj(n))"

    mosaic:
      # Mosaic tile layout — irregular colored tiles with grout lines.
      # Flat cell sequence (like voronoi), tiles have varied shapes.
      payload_wordlist: viridis
      rules:
        sentence:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "HEADER CELLS Dot[end]"
              weight: 1.0
              lambda: "λn:(e->t). n"

        HEADER:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "Prefix[mosaic] Aux[size] Aux[palette] Conj"
              weight: 1.0
              lambda: "λn:(e->t). n"

        CELLS:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "CELL"
              weight: 0.1
              lambda: "λn:(e->t). n"
            - production: "CELL CELLS"
              weight: 0.9
              lambda: "λn:(e->t). n"

        CELL:
          # Mosaic cells use mixed shapes for visual variety.
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "N Adj"
              weight: 0.70
              lambda: "λn:(e->t). λadj:(e->e). adj(n)"
            - production: "Cop Adj"
              weight: 0.10
              lambda: "λadj:(e->e). λcop:((e->t)->(e->t)). cop(adj)"
            - production: "Adv N Adj"
              weight: 0.15
              lambda: "λn:(e->t). λadj:(e->e). λadv:((e->t)->(e->t)). adv(adj(n))"
            - production: "Modal N Adj"
              weight: 0.05
              lambda: "λn:(e->t). λadj:(e->e). λmod:((e->t)->(e->t)). mod(adj(n))"

    constellation:
      # Constellation layout — colored dots on a dark background.
      # Connecting lines and background gradient are cover.
      payload_wordlist: viridis
      rules:
        sentence:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "HEADER CELLS Dot[end]"
              weight: 1.0
              lambda: "λn:(e->t). n"

        HEADER:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "Prefix[constellation] Aux[size] Aux[palette] Conj"
              weight: 1.0
              lambda: "λn:(e->t). n"

        CELLS:
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "CELL"
              weight: 0.1
              lambda: "λn:(e->t). n"
            - production: "CELL CELLS"
              weight: 0.9
              lambda: "λn:(e->t). n"

        CELL:
          # Constellation cells are always dots (the shape IS the dialect).
          lambda: "λn:(e->t). n"
          cfg_productions:
            - production: "N Adj"
              weight: 0.80
              lambda: "λn:(e->t). λadj:(e->e). adj(n)"
            - production: "Cop Adj"
              weight: 0.05
              lambda: "λadj:(e->e). λcop:((e->t)->(e->t)). cop(adj)"
            - production: "Adv N Adj"
              weight: 0.10
              lambda: "λn:(e->t). λadj:(e->e). λadv:((e->t)->(e->t)). adv(adj(n))"
            - production: "Modal N Adj"
              weight: 0.05
              lambda: "λn:(e->t). λadj:(e->e). λmod:((e->t)->(e->t)). mod(adj(n))"

    # -- Palette selection ------------------------------------------------
    #
    # Palettes are wordlists, not dialects. Combine any layout dialect
    # with any palette via --wordlist:
    #
    #   --language image --dialect voronoi                   (default: viridis)
    #   --language image --dialect voronoi --wordlist plasma
    #   --language image --dialect grid --wordlist rocket
    #
    # Available palettes (payload_<name>.yaml):
    #   viridis  (128 colors, CC0)     — deep purple through teal to yellow-green
    #   plasma   (128 colors, CC0)     — dark purple through magenta to orange-yellow
    #   inferno  (128 colors, CC0)     — near-black through red-orange to bright yellow
    #   magma    ( 64 colors, CC0)     — near-black through purple-pink to pale white
    #   cividis  (128 colors, CC0)     — dark blue through olive-grey to yellow (CVD-safe)
    #   turbo    (  8 colors, Apache)  — dark blue through rainbow to dark red
    #   mako     ( 64 colors, BSD-3)   — deep purple-black through teal to pale cyan
    #   rocket   ( 32 colors, BSD-3)   — near-black through magenta to pale white
